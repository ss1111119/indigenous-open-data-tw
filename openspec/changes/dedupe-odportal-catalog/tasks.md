## 1. 分群

- [ ] 1.1 以共用資源指標建立連通分量分群，並斷言無三筆以上的群；指標指向清單外的 id 時報出該 id 與來源列並中止（Group cross-listed datasets by connected components，對應設計決策「以連通分量分群，不做兩兩配對」）。驗證：對 763 筆執行後得 603 個單筆群與 80 個雙筆群；以人工在暫存副本中加入第三筆互指，確認報出群成員且未產出輸出

## 2. 合併

- [ ] 2.1 合併後的資源集合取各成員資源網址的聯集，並依聯集重新計數 `資源數` 與 `資源總數`；聯集數小於任一成員原本資源數時報出該群並中止（Merge resources as a union，對應設計決策「資源取聯集，不選單一贏家」）。驗證：`10730-09-01-2 臺中市原住民權益及福利宣導統計` 合併後資源數為 183（非 18）；輸出的資源網址聯集與輸入 763 筆的完全相同，不多不少
- [ ] 2.2 依實測衝突分佈解析各欄位：`提供機關` 取有值那側、`名稱` 取 `提供機關` 有值那側、`來源平台` 與 `格式` 取排序後聯集、`授權` 取具體值而非 `UNKNOWN` 或空、`最後更新` 取較新者、`分級` 取較可用者；`提供機關` 兩側皆有值且不同時報出雙方值並中止（Resolve field conflicts by measured rule，對應設計決策「各欄位的合併規則依實測衝突分佈決定」）。驗證：80 個合併列的 `提供機關` 全部非空；`來源平台` 為兩平台並列；桃園那 6 筆名稱不同者取到地方入口網的寫法
- [ ] 2.3 新增 `合併筆數` 與 `合併來源` 兩欄記錄來源，未合併者分別為 1 與自身 id（Merged rows record their provenance，對應設計決策「保留原清單，另出新檔」）。驗證：`合併筆數` 為 2 的列恰 80 列、`合併筆數` 總和恰 763；任一合併列的 `合併來源` 含兩個 id 且已排序

## 3. 分級覆蓋範圍

- [ ] 3.1 新增布林欄 `分級未涵蓋新增資源`，於聯集後資源數超過任一成員原本數量的列為真；全程不發出任何網路請求（Flag rows whose grade does not cover the merged resources，對應設計決策「分級未涵蓋新增資源以旗標欄標示，不重新探測」）。驗證：該欄為真者恰 23 列；以原始碼檢查確認去重模組未匯入 urllib、requests、httpx、socket

## 4. 輸出

- [ ] 4.1 輸出 `catalog/odportal-deduped.csv` 共 683 列，保留原清單全部既有欄位並附加三個新欄；`catalog/odportal-763-graded.csv` 不得被修改；找不到 `data/raw/catalog/odportal-resources.json` 時報出路徑與重新取得的指令並中止。驗證：輸出列數為 683、欄位為原欄位加三欄；執行前後 `catalog/odportal-763-graded.csv` 的雜湊值不變
- [ ] 4.2 去重為輸入的確定性函式，重跑產生位元組相同的輸出（Deduplication is reproducible）。驗證：連續執行兩次並比對輸出檔雜湊值相同

## 5. 不變量測試

- [ ] 5.1 建立不變量測試，釘住四項約束：輸出資源網址聯集等於輸入聯集；輸出 683 列且 `合併筆數` 為 2 者 80 列、總和 763；`分級未涵蓋新增資源` 為真者 23 列；合併列的 `提供機關` 皆非空（Merge resources as a union、Merged rows record their provenance）。驗證：測試套件全數通過，且不需要 `data/raw/` 即可執行「筆數關係」那幾項
- [ ] 5.2 對「不掉資源」與「筆數關係」兩項不變量分別注入故障，確認對應測試會失敗且不互相遮蔽（Invariant tests detect a seeded fault）。驗證：兩項注入各自造成且僅造成對應測試失敗

## 6. 文件

- [ ] 6.1 更新 `catalog/品質分級報告.md`，說明去重後的 683 筆清單、兩份清單各自的用途與關係，以及 23 列的分級覆蓋限制與為何不重新探測。驗證：報告中的每個數字與實際輸出相符，且明確指出哪一份是編目的最新狀態
- [ ] 6.2 更新 `docs/來源盤點.md` 第八節，將去重項目標記為已完成並指向 `catalog/odportal-deduped.csv`；第五節補記去重後的實際筆數。驗證：兩處敘述與實際產出一致，且不含尚未做到的承諾
