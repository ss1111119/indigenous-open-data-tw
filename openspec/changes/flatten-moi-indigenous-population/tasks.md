## 1. 取檔

- [x] 1.1 實作逐頁取得單一期別，依來源回報的頁數請求全部頁面；收集列數與來源回報總列數不符時報出期別與兩個數字並中止（Retrieve each period page by page）。驗證：取 11507 得 4 頁共 7,781 列，與來源回報的總列數相符；人為把頁數上限改為 1 時報錯而非靜默截斷
- [x] 1.2 區分「尚未發布」與「請求失敗」：來源回「查無資料」記為未發布並繼續其餘期別，其他失敗則報出期別與失敗原因且不記為未發布（Distinguish an unpublished period from a failed request）。驗證：11508 記為未發布、11507 正常取得；斷網執行時報出的是請求失敗而非未發布
- [x] 1.3 取檔為可重跑：已有完整取檔紀錄的期別不重複請求（Retrieval is repeatable）。驗證：連續執行兩次，第二次不發出任何請求且本機檔案未變動

## 2. 跨期結構

- [x] 2.1 建立欄位註冊表，以中文族名為正規值並列出各期的來源欄名別名（鄒族對映 `tsou` 與 `cou`、卑南族對映 `puyuma` 與 `pinuyumayan`、拉阿魯哇族對映 `hlaaluaavu` 與 `hlaalua`），同時描述兩種欄名前綴（Normalize people names across the source revision，對應設計決策「以註冊表描述跨期欄名與族名改版」）。驗證：11401 與 11507 兩期解析後的 `族別` 標籤集合相同（扣除平埔族群）；解析程式中不存在以期別分支的邏輯
- [x] 2.2 偵測每期的欄名版本；無法判定版本時報出該期實際的欄位前綴並中止；出現註冊表未描述的資料欄時報出期別與欄名並中止（An undescribed column appears）。驗證：11401 判定為舊版 115 欄、11412 判定為新版 162 欄；在暫存副本中插入一個假欄位後確認報錯且未產出該期結果

## 3. 驗證與聚合

- [x] 3.1 在聚合前對每期的全部村里列逐列檢查七組恆等式：(A) 總計等於男加女、(B) 總計等於平地小計加山地小計加平埔小計、(C) 總計等於該期族別聯集之和、(D) 平地小計等於其族別清單之和、(E) 山地小計等於其族別清單之和、(F) 平埔小計等於其族別清單之和、(G) 每一族的頂層值等於該族在各身分別之和；族別清單按身分別取自註冊表而非共用同一份；任一不符時報出期別、村里代碼、不符的等式與差值並中止，且不寫出該期的聚合結果與取檔紀錄（Verify every period before discarding detail）。驗證：11507 全 7,781 列七組恆等式零容差通過；人為改動一格數值後確認報錯且未產出該期任何檔案
- [x] 3.2 於記憶體內聚合到鄉鎮市區後只落地聚合結果，村里層級不得寫入磁碟（Aggregate to district level and discard village detail，對應設計決策「於取檔階段聚合到鄉鎮市區，捨棄村里明細」）。驗證：11507 聚合後為 368 個鄉鎮市區、全國總計 638,466（平地 298,327、山地 340,139、平埔 0）；`data/raw/moi/` 下無任何檔案含村里名稱或村里代碼
- [x] 3.3 每期寫出取檔紀錄，內容含期別、來源回報總列數、實際取得列數、頁數、聚合後鄉鎮市區數、七組恆等式的檢查結果、欄名版本、取檔時間（Record what was retrieved and verified，對應設計決策「取檔留下 manifest，使捨棄後仍可查核」）。驗證：11507 的紀錄可讀出上述每一項，且欄名版本記為新版

## 4. 長表建置

- [x] 4.1 輸出長表，欄位為 `期別`／`縣市`／`鄉鎮市區`／`身分別`／`族別`／`性別`／`人數` 共 7 欄，每欄在每列皆非空，`人數` 為非負整數（Emit a district-level population long table）。驗證：讀入輸出檔後無任何欄位為空、`人數` 皆為非負整數；11507 的列聚合後為 368 個鄉鎮市區且總計 638,466
- [x] 4.2 長表只保留族別 × 性別 × 身分別的最細組合，總計欄與身分別小計不入長表（Exclude aggregate columns from the long table，對應設計決策「長表只存明細，彙總值用於驗證後捨棄」）。驗證：單一期別跨全部維度加總等於來源回報的該期全國總計；輸出中不存在代表總計的 `族別`／`性別`／`身分別` 取值
- [x] 4.3 `身分別` 為平埔原住民的列只在來源帶平埔欄位的期別產出；11411 及之前不產列，11412 起產列且人數為 0；若在 11412 之前發現平埔欄位則報出期別並中止（Emit plains-plain rows only for periods that carry them，對應設計決策「平埔列只在來源帶該欄位的期別產出」）。驗證：輸出中 `身分別` 為平埔原住民的最小期別為 11412；11507 的平埔列人數全為 0
- [x] 4.4 行政區名一律用「臺」，`縣市` 與 `鄉鎮市區` 由來源的 `site_id` 拆出（Administrative names use the orthodox form）。驗證：輸出中 `縣市` 與 `鄉鎮市區` 皆不含「台」字；11507 的 `縣市` 取值數與實際縣市數相符
- [x] 4.5 建置只讀本機檔案且為確定性函式，重跑產生位元組相同的輸出；manifest 與聚合結果的期別集合不一致時報出差集並中止（Builds are reproducible and offline）。驗證：連續建置兩次雜湊值相同；以原始碼檢查確認建置模組未匯入 urllib、requests、httpx、socket

## 5. 不變量測試

- [x] 5.1 建立不變量測試，釘住四項約束：每個「期別 × 鄉鎮市區 × 身分別」群恰有該身分別的族別數乘以性別數之列（維度交叉完整；**不可寫成「依族別加總等於依身分別加總」——該式恆真、注入故障不會失敗**）；`族別` 標籤集合跨改版一致（扣除平埔族群）；`身分別` 為平埔原住民的期別下界為 11412；長表期別集合等於取檔紀錄的期別集合（Cross-period invariants hold，對應設計決策「不變量測試釘跨期與跨維度約束」）。驗證：測試套件全數通過，且不需要重新取檔即可執行
- [x] 5.2 對「維度交叉完整」與「平埔列的期別下界」兩項不變量分別注入故障，確認對應測試會失敗；若兩者無法互相獨立，於測試中明寫獨立性成立的方向而不假裝雙向成立（Invariant tests detect a seeded fault）。驗證：兩項注入各自造成對應測試失敗

## 6. 文件

- [x] 6.1 撰寫長表 schema 文件，說明 7 個欄位、`身分別` 三個取值的法源差異、平埔列的期別下界及其法源背景，並明確警告 11508 起平埔轉為非零時數字上升不等於原住民人口成長。驗證：文件涵蓋全部 7 個欄位與 3 個身分別取值，且含一段可直接執行的正確用法範例
- [x] 6.2 更新 `README.md` 現況表與兩層表格，將第二張長表列為完成並指向輸出檔與 schema 文件；更新 `docs/來源盤點.md`，補記戶政司 ODRP 這條路徑、11412 改版、以及村里粒度的倫理處置與其實測依據。驗證：兩份文件的敘述與實際產出一致，且不含尚未做到的承諾
