## 1. 對照表

- [ ] 1.1 建立教育程度對照表，把 106–107 的 23 個縮寫對映到 108–114 的全稱，並在 `二畢`（二、三專畢業）與 `後二畢`（五專畢業）兩筆附註其字面意思與實際涵蓋不同；對照表未列的值視為阻斷性錯誤（Normalize education levels to their full spellings，對應設計決策「教育程度以全稱為正規值」）。驗證：23 個縮寫與 23 個全稱一對一無剩餘；以人工置入 `碩士後畢業` 這個不存在的值，確認報出年別與該值且未產出該年結果
- [ ] 1.2 建立欄名變體對照，涵蓋身分別欄的三種名稱（`aborigine`／`原住民身分`／`indigenous`）與其餘七個維度欄的中英兩套寫法，並在比對鍵時忽略前導 BOM；無法判定時報出該年實際欄名並中止（Resolve the status column across its naming variants）。驗證：106（帶 BOM、無 `district_code`）、113（全中文）、114（`indigenous`）三年皆可解析出身分別欄；以人工把欄名全部改成不認識的名稱，確認報錯並中止

## 2. 取檔

- [ ] 2.1 沿用既有取檔基礎處理 `ODRP026` 的 9 個年別：分頁抓完、重試暫時性故障、區分「尚未發布」與「請求失敗」、已有完整紀錄的年別不重複請求（沿用 `moi-population-ingest` 既有要求，對應設計決策「沿用既有的取檔基礎，一般化驗證的接口」）。驗證：114 取得 1,069,776 列且與來源回報的總列數相符；115 記為尚未發布；連續執行兩次第二次不發出任何請求
- [ ] 2.2 以維度交叉完整性驗證每年：列數必須等於「村里數 × 23 教育程度 × 2 性別 × 該年身分別數」；不整除時找出並報出缺漏或多出的維度組合（Verify every period before discarding detail 的修改版，對應設計決策「以維度交叉完整性取代來源缺少的彙總驗證」）。驗證：106、107、108、109、110、111、112、114 八年整除；113 報出恰缺 1 個維度組合並指出是哪一個
- [ ] 2.3 把該年推得的村里數與 `ODRP018` 同年任一期的村里數比對，相符與否都記入取檔紀錄；不符時報出兩個數字與比對用的期別，但**不中止**（Cross-dataset village counts are reconciled，對應設計決策「以 `ODRP018` 的村里數做跨資料集對帳」）。驗證：112 推得 7,748 與 `ODRP018` 的 11306 相符、114 推得 7,752 與 11412 相符，兩者皆記入紀錄
- [ ] 2.4 於記憶體內聚合到鄉鎮市區後只落地聚合結果，村里層級不得寫入磁碟；每年寫出取檔紀錄，含年別、來源回報列數、取得列數、頁數、鄉鎮市區數、交叉完整性結果、缺漏的維度組合（若有）、村里數對帳結果、欄名變體、教育程度寫法、取檔時間（沿用 `moi-population-ingest` 的村里捨棄與紀錄要求）。驗證：`data/raw/moi-education/` 下無任何檔案含村里名稱或村里代碼；113 的紀錄可讀出缺漏的維度組合

## 3. 長表建置

- [ ] 3.1 輸出長表，欄位為 `統計年`／`縣市`／`鄉鎮市區`／`身分別`／`教育程度`／`性別`／`人數` 共 7 欄，每欄在每列皆非空，`人數` 為非負整數（Emit a district-level education-attainment long table）。驗證：讀入輸出檔後無空值、`人數` 皆非負；`統計年` 集合恰為 106–114
- [ ] 3.2 `教育程度` 輸出恰為 23 個全稱、不含任何縮寫；`身分別` 在 114 為 3 種、其餘年別為 2 種（Both spellings converge、Status values follow the year）。驗證：輸出的 `教育程度` 集合與對照表的全稱集合完全相同；114 的身分別數為 3、113 為 2
- [ ] 3.3 行政區名一律用「臺」，`縣市` 與 `鄉鎮市區` 由來源的區域別欄拆出（沿用既有的行政區正規化）。驗證：輸出中 `縣市` 與 `鄉鎮市區` 皆不含「台」字，且 `縣市` 取值皆在既有縣市清單內
- [ ] 3.4 113 缺的維度組合在長表中不產列，不以 0 補值（A missing dimension combination is reported, not filled，對應設計決策「113 缺的列報出但不補」）。驗證：113 的列數比完整交叉積少 1；該缺漏組合在長表中查不到列
- [ ] 3.5 建置只讀本機檔案且為確定性函式，gzip 標頭的 mtime 固定為 0，重跑產生位元組相同的輸出（Builds are reproducible and offline）。驗證：間隔數秒後連續建置兩次雜湊值相同；以原始碼檢查確認建置模組未匯入 urllib、requests、httpx、socket

## 4. 不變量測試

- [ ] 4.1 建立不變量測試，釘住四項約束：每個「統計年 × 鄉鎮市區 × 身分別」群的列數等於 23 × 2（113 的缺漏組合除外）；`教育程度` 集合跨年一致且為全稱；114 有 3 種身分別而其餘年別 2 種；長表年別集合等於取檔紀錄的年別集合（Emit a district-level education-attainment long table、Both spellings converge）。驗證：測試套件全數通過，且不需要重新取檔即可執行
- [ ] 4.2 對「維度交叉完整」與「教育程度正規化」兩項不變量分別注入故障，確認對應測試會失敗；若兩者無法互相獨立，於測試中明寫獨立性成立的方向而不假裝雙向成立（Invariant tests detect a seeded fault）。驗證：兩項注入各自造成對應測試失敗

## 5. 文件

- [ ] 5.1 撰寫長表 schema 文件，說明 7 個欄位、23 個教育程度值、身分別隨年別而異、113 缺 1 列的事實與為何不補，並明確說明本表是**存量**（15 歲以上已完成的教育程度）而教育部長表是**在學**，兩者母體不同不可相除。驗證：文件涵蓋全部 7 個欄位與 23 個教育程度值，且含一段可直接執行的正確用法範例
- [ ] 5.2 更新 `README.md` 現況表與兩層表格，將第三張長表列為完成並指向輸出檔與 schema 文件；更新 `docs/來源盤點.md` 的戶政司段落，補記 ODRP026 的四種結構變體、來源無彙總欄的驗證替代方案、以及跨資料集村里數對帳。驗證：兩份文件的敘述與實際產出一致，且不含尚未做到的承諾
