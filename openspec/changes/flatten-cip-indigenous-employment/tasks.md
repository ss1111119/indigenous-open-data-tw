## 1. 註冊表

- [ ] 1.1 建立資料集註冊表，描述 4 個識別碼、各自的主題與切法、母體欄名、以及每個主題的兩個合法狀態值（勞動力參與情形為勞動力／非勞動力；就失業情形為就業／失業）；同時宣告 106 為排除年度並附排除理由（Status values follow the topic，對應設計決策「攤平為「母體 × 狀態 × 性別」的長格式，人數與百分比並列」）。驗證：4 個識別碼皆有條目；解析程式中不存在以識別碼分支的邏輯；`狀態` 出現註冊表未宣告的值時報出主題與該值並中止
- [ ] 1.2 驗證回應包裝：確認頂層為 `[{success, result:{fields, records}}]` 後才取值，不符時報出實際頂層鍵並中止（The response envelope is validated before parsing）。驗證：正常回應可取出 records 與 fields；以人工把回應改成純陣列後確認報出實際頂層鍵，且不會產出只有一列包裝鍵的表

## 2. 取檔

- [ ] 2.1 取得 4 個資料集並落地至 `data/raw/cip-employment/`；已取得者不重複請求；4 筆的年度集合不一致時報出各自集合並中止（Emit an employment long table across the usable years）。驗證：4 筆皆取得且年度集合皆為 103–106、108–112；連續執行兩次第二次不發出任何請求
- [ ] 2.2 驗證彙總恆等式：對每個可用年度逐列檢查「總計 = 男 + 女」與「總計 = 狀態二分之和」，絕對容差 10 人；超過即報出年度、類別、項目別、恆等式與誤差並中止；同時記錄每年的實際最大誤差（Aggregate identities hold within a measured tolerance，對應設計決策「彙總恆等式以絕對容差 10 人驗證，並記錄實際誤差」）。驗證：8 個可用年度全部通過且記錄的最大誤差為 105 年的 8.00；人工把某列的男加女改動 20 人後確認報錯並指出該列
- [ ] 2.3 對 106 年執行同樣的恆等式檢查，**預期其超過容差**；若未超過則報錯，因為那與「106 年錯位」的排除依據矛盾（The excluded year still fails its identities，對應設計決策「106 年整年排除，不嘗試修復」）。驗證：106 年的「總計 = 男 + 女」最大誤差為 117,516.09，遠超容差，檢查如預期失敗且該事實記入取檔紀錄

## 3. 長表建置

- [ ] 3.1 把 19 個寬指標欄攤平為「母體 × 狀態 × 性別」的長格式，輸出欄位為 `年度`／`主題`／`切法`／`類別`／`項目別`／`性別`／`狀態`／`人數`／`百分比`／`樣本數` 共 10 欄；維度欄每列皆非空，三個量度欄可為空（Emit an employment long table across the usable years）。驗證：讀入輸出檔後 7 個維度欄無空值；`人數` 為浮點型別（來源是非整數估計值）；不做任何空間聚合，`類別`／`項目別` 照來源原樣保留（對應設計決策「不做鄉鎮市區聚合，因為來源就沒有那麼細」——本來源最細只到縣市與統計區域，無村里資料）
- [ ] 3.2 長表不含 106 與 107 年的任何列，年度集合恰為 103、104、105、108、109、110、111、112（Emit an employment long table across the usable years，對應設計決策「106 年整年排除，不嘗試修復」）。驗證：年度集合恰為那 8 個值；篩 `年度 == 106` 得空集合
- [ ] 3.3 `狀態` 與 `性別` 為總計的列不入長表，只保留最細組合（Aggregate totals are verified then discarded，對應設計決策「狀態與性別的「總計」用於驗證後捨棄」）。驗證：`狀態` 取值恰為四種（勞動力／非勞動力／就業／失業）、`性別` 恰為男與女；輸出中不存在任何代表總計的取值
- [ ] 3.4 `樣本數` 隨列保留，且樣本數為空的列不得被丟棄（Sample sizes are retained）。驗證：輸出含 `樣本數` 欄；來源中樣本數為空的列在輸出中仍存在
- [ ] 3.5 建置只讀本機檔案且為確定性函式，重跑產生位元組相同的輸出（Builds are reproducible and offline）。驗證：連續建置兩次雜湊值相同；以原始碼檢查確認建置模組未匯入 urllib、requests、httpx、socket

## 4. 不變量測試

- [ ] 4.1 建立不變量測試，釘住五項約束：年度集合恰為 8 個可用年度且不含 106／107；每個主題的 `狀態` 恰為其註冊表宣告的兩個值且不跨主題出現；長表不含 `狀態` 或 `性別` 為總計的列；每個「年度 × 主題 × 切法 × 類別 × 項目別」群恰有 2 狀態 × 2 性別共 4 列；`樣本數` 欄存在且空值列未被丟棄（Status values follow the topic、No total rows survive）。驗證：測試套件全數通過，且不需要重新取檔即可執行
- [ ] 4.2 對「106 年不得出現」與「不得含總計列」兩項不變量分別注入故障，確認對應測試會失敗；若兩者無法互相獨立，於測試中明寫獨立性成立的方向而不假裝雙向成立（Invariant tests detect a seeded fault）。驗證：兩項注入各自造成對應測試失敗

## 5. 文件

- [ ] 5.1 撰寫長表 schema 文件，說明 10 個欄位、兩個主題各自的狀態值、**本表是抽樣調查（人數為非整數估計值、須看樣本數）**、106 與 107 兩個缺口的性質不同、以及**百分比不可加總**且捨棄總計後要算率必須從人數重算。驗證：文件涵蓋全部 10 個欄位與四個狀態值，含一段可直接執行的正確用法範例，並含一段明確標示為錯誤用法的反例（加總百分比）
- [ ] 5.2 更新 `README.md` 現況表與兩層表格，將第四張長表列為完成並指向輸出檔與 schema 文件；更新 `docs/來源盤點.md` 的就業段落，補記 4 筆核心表的結構、106 年錯位的完整證據、以及容差 10 人的依據。驗證：兩份文件的敘述與實際產出一致，且不含尚未做到的承諾
